<section class="section cta-band">
    <div class="container cta-band__inner reveal"><div><p class="eyebrow"><?= h($ctaEyebrow ?? 'Start a conversation') ?></p><h2><?= h($ctaTitle ?? 'Let’s identify the right next step.') ?></h2><p><?= h($ctaText ?? 'Tell us what you are trying to improve, build or teach. We will help clarify a practical direction.') ?></p></div><div class="cta-band__actions"><a class="button" href="<?= h(url($ctaUrl ?? '/request-consultation')) ?>"><?= h($ctaLabel ?? 'Request a Consultation') ?></a><?php if (!empty($ctaSecondaryUrl) && !empty($ctaSecondaryLabel)): ?><a class="button button--outline-light" href="<?= h(safe_external_url($ctaSecondaryUrl)) ?>" target="_blank" rel="noopener noreferrer"><?= h($ctaSecondaryLabel) ?></a><?php endif; ?></div></div>
</section>
